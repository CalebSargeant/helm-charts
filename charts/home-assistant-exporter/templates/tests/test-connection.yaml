apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "home-assistant-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "home-assistant-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "home-assistant-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
