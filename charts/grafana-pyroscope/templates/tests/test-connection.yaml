apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-pyroscope.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-pyroscope.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-pyroscope.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
