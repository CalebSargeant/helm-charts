apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-pyroscope.fullname" . }}
  labels:
    {{- include "grafana-pyroscope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-pyroscope.selectorLabels" . | nindent 4 }}
