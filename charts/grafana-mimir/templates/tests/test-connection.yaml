apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-mimir.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-mimir.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-mimir.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
