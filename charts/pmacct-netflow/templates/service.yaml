apiVersion: v1
kind: Service
metadata:
  name: {{ include "pmacct-netflow.fullname" . }}
  labels:
    {{- include "pmacct-netflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pmacct-netflow.selectorLabels" . | nindent 4 }}
