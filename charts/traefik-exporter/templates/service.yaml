apiVersion: v1
kind: Service
metadata:
  name: {{ include "traefik-exporter.fullname" . }}
  labels:
    {{- include "traefik-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traefik-exporter.selectorLabels" . | nindent 4 }}
