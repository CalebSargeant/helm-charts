apiVersion: v1
kind: Service
metadata:
  name: {{ include "plex-exporter.fullname" . }}
  labels:
    {{- include "plex-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plex-exporter.selectorLabels" . | nindent 4 }}
