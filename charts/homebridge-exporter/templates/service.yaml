apiVersion: v1
kind: Service
metadata:
  name: {{ include "homebridge-exporter.fullname" . }}
  labels:
    {{- include "homebridge-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homebridge-exporter.selectorLabels" . | nindent 4 }}
