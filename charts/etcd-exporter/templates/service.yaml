apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd-exporter.fullname" . }}
  labels:
    {{- include "etcd-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etcd-exporter.selectorLabels" . | nindent 4 }}
