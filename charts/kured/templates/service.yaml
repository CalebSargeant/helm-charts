apiVersion: v1
kind: Service
metadata:
  name: {{ include "kured.fullname" . }}
  labels:
    {{- include "kured.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kured.selectorLabels" . | nindent 4 }}
