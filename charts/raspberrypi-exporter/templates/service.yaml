apiVersion: v1
kind: Service
metadata:
  name: {{ include "raspberrypi-exporter.fullname" . }}
  labels:
    {{- include "raspberrypi-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "raspberrypi-exporter.selectorLabels" . | nindent 4 }}
