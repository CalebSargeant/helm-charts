apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-tempo.fullname" . }}
  labels:
    {{- include "grafana-tempo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-tempo.selectorLabels" . | nindent 4 }}
