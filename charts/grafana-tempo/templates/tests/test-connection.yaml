apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-tempo.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-tempo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-tempo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
