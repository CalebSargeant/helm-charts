apiVersion: v1
kind: Service
metadata:
  name: {{ include "your-spotify.fullname" . }}
  labels:
    {{- include "your-spotify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "your-spotify.selectorLabels" . | nindent 4 }}
