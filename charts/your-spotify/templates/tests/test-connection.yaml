apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "your-spotify.fullname" . }}-test-connection"
  labels:
    {{- include "your-spotify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "your-spotify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
