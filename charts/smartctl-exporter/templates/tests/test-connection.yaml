apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartctl-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "smartctl-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartctl-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
