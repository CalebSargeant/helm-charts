apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartctl-exporter.fullname" . }}
  labels:
    {{- include "smartctl-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartctl-exporter.selectorLabels" . | nindent 4 }}
